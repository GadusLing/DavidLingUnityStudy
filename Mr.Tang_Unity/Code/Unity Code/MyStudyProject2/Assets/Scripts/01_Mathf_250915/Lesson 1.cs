using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lesson1
{
    //Math
    //Mathf

    //clamp(x,min,max)=min(max(x,min),max)


    //lerp(a,b,t)
    //result=a+(b−a)×t
    //result=(1−t)×a+t×b
    //可以理解成t和b的权重直接相关，t越大，b的权重越大，t越小，b的权重越小(a就越大)
}
