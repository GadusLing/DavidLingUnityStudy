using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// 1. 挂在空物体上的脚本，运行时实例化坦克预设体
/// 2. A脚本挂A对象，B脚本挂B对象，在A脚本Start中让B对象上的B脚本失活
/// 3. A对象挂脚本，通过Inspector设置让B对象改名、失活、延迟删除、立即删除（用枚举控制）
/// </summary>
public class OBJMemberVariable : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
